import { ShowToRolesDirective } from './showtoroles.directive';

describe('ShowToRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowToRolesDirective();
    expect(directive).toBeTruthy();
  });
});
